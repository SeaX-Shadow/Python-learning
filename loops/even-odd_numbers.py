# The third argument makes the range funcion add 2 to the value. -1
even_numbers = list(range(2, 11, 2))
print(even_numbers)
# Same thing as the first one.
odd_numbers = list(range(1, 10, 2))
print(odd_numbers)